async function renderProducts(){const grid=document.querySelector('#productGrid');try{const res=await fetch('./data/products.json');const products=await res.json();grid.innerHTML=products.map(p=>`<article class="product-card"><img class="product-image" src="${p.image}" alt="${p.name}"><div class="product-body"><span class="badge">Sourdough</span><h3>${p.name}</h3><p>${p.description}</p><div class="price">${new Intl.NumberFormat('th-TH').format(p.price)} ฿</div></div></article>`).join('')}catch(err){grid.innerHTML='<p>Unable to load products.</p>'}}renderProducts();